swagger: '2.0'
info:
  $ref: ./commons/info.yaml
host: app.launchdarkly.com
basePath: /api/v2
schemes:
  - https
consumes:
  - application/json
produces:
  - application/json
securityDefinitions:
  Token:
    type: apiKey
    name: Authorization
    in: header
    description: https://app.launchdarkly.com/settings#/tokens
security:
  - Token: []
paths:
  $ref: ./commons/paths.yaml
definitions:
  $ref: ./commons/definitions.yaml
responses:
  $ref: ./commons/responses.yaml
parameters:
  $ref: ./commons/parameters.yaml